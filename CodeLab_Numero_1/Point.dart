// Definición de la clase 'Point' que representa un punto en un plano 2D
class Point {
  // Propiedades finales que representan las coordenadas x e y del punto
  final int x;
  final int y;

  // Constructor constante que inicializa las coordenadas del punto
  const Point(this.x, this.y);
}
